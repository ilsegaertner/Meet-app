import React from "react";

const NumberOfEvents = () => {
  return (
    <div id="number-of-events">
      <input type="number" className="event-numbers" value={32} />
    </div>
  );
};

export default NumberOfEvents;
